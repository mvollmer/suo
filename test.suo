(set! count (make-vector 10 '()))
(hashq-set! count 'foo 12)
(hashq-set! count 'bar 14)
count
(set-hashq-vectors (cons count (get-hashq-vectors)))

(fac 20)

;;(alist->hashq! (hashq->alist! count) count)
count
(hashq-ref count 'foo)

(sys:halt)
